<UserControl x:Class="Presentation.Wpf.Views.RoleSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.Wpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock Text="Vælg rolle:" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20" />

        <!-- Horisontal layout til knapper -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Employee" 
                    Width="200" Height="40"
                    Margin="0,0,10,0"
                    Command="{Binding SelectEmployeeCommand}" />

            <Button Content="Asset Manager" 
                    Width="200" Height="40"
                    Command="{Binding SelectAssetManagerCommand}" />
        </StackPanel>
    </StackPanel>

</UserControl>
