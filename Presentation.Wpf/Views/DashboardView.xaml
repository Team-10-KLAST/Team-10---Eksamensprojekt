<UserControl x:Class="Presentation.Wpf.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Presentation.Wpf.Views"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="1000"
             Background="{StaticResource PageBackgroundBrush}">

    <!-- Lokale styles kun til Dashboard -->
    <UserControl.Resources>

        <!-- Store kort øverst: "Register device" / "Register employee" -->
        <Style x:Key="QuickActionCardBorderStyle"
               TargetType="Border"
               BasedOn="{StaticResource CardBorderStyle}">
            <Setter Property="Padding" Value="24" />
        </Style>

        <!-- De fire status-bokse (Requests, Planned, Ordered, Ready) -->
        <Style x:Key="StatusColumnCardBorderStyle"
               TargetType="Border"
               BasedOn="{StaticResource CardBorderStyle}">
            <Setter Property="Padding" Value="20" />
        </Style>

        <!-- De små hvide kort inde i hver status-boks -->
        <Style x:Key="StatusItemCardBorderStyle"
               TargetType="Border"
               BasedOn="{StaticResource CardBorderStyle}">
            <Setter Property="Padding" Value="12" />
            <Setter Property="Margin" Value="0,8,0,0" />
            <Setter Property="CornerRadius" Value="12" />
        </Style>

    </UserControl.Resources>

    <!-- Dashboardet skaleres, når vinduet ændrer størrelse -->
    <Grid x:Name="Root">
        <Viewbox Stretch="Uniform">
        <Grid Width="1000" Height="600" Margin="24">

            <!-- Rækker: 0 = titel, 1 = "Quick actions", 2 = quick cards, 3 = status-kolonner -->
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- 1. Sideheader -->
            <TextBlock Grid.Row="0"
                       Text="Dashboard"
                       Style="{StaticResource HeaderTextStyle}"
                       VerticalAlignment="Center" />

            <!-- 2. "Quick actions" label -->
            <TextBlock Grid.Row="1"
                       Margin="0,24,0,8"
                       Text="Quick actions"
                       FontFamily="{StaticResource PrimaryFont}"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource PrimaryTextBrush}" />

            <!-- 3. Quick action cards (Register device / Register employee) -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Quick action: Register device -->
                <Border Grid.Column="0"
                        Style="{StaticResource QuickActionCardBorderStyle}"
                        Margin="0,0,16,24">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0"
                                Style="{StaticResource StatusIconCircleStyle}"
                                Background="{StaticResource PrimaryActionBrush}">
                            <TextBlock Text="🖥"
                                       FontSize="20"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />
                        </Border>

                        <StackPanel Grid.Column="1"
                                    Margin="16,0,16,0"
                                    VerticalAlignment="Center">
                            <TextBlock Text="Register device"
                                       FontFamily="{StaticResource PrimaryFont}"
                                       FontSize="18"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource PrimaryTextBrush}" />
                            <TextBlock Text="Create a new asset and (optionally) assign an owner"
                                       FontFamily="{StaticResource PrimaryFont}"
                                       FontSize="13"
                                       Foreground="{StaticResource SecondaryTextBrush}" />
                        </StackPanel>

                        <Button Grid.Column="2"
                                Content="→"
                                FontSize="20"
                                VerticalAlignment="Center"
                                Foreground="{StaticResource SecondaryTextBrush}"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Background="Transparent"
                                BorderThickness="0"
                                Command="{Binding OpenRegisterDeviceCommand}" />
                    </Grid>
                </Border>

                <!-- Quick action: Register employee -->
                <Border Grid.Column="1"
                        Style="{StaticResource QuickActionCardBorderStyle}"
                        Margin="16,0,0,24">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0"
                                Style="{StaticResource StatusIconCircleStyle}"
                                Background="{StaticResource PrimaryActionBrush}">
                            <TextBlock Text="👤"
                                       FontSize="20"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />
                        </Border>

                        <StackPanel Grid.Column="1"
                                    Margin="16,0,16,0"
                                    VerticalAlignment="Center">
                            <TextBlock Text="Register employee"
                                       FontFamily="{StaticResource PrimaryFont}"
                                       FontSize="18"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource PrimaryTextBrush}" />
                            <TextBlock Text="Add a new employee profile for assignments"
                                       FontFamily="{StaticResource PrimaryFont}"
                                       FontSize="13"
                                       Foreground="{StaticResource SecondaryTextBrush}" />
                        </StackPanel>

                        <Button Grid.Column="2"
                                Content="→"
                                FontSize="20"
                                VerticalAlignment="Center"
                                Foreground="{StaticResource SecondaryTextBrush}"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Background="Transparent"
                                BorderThickness="0"
                                Command="{Binding OpenRegisterEmployeeCommand}" />
                    </Grid>
                </Border>
            </Grid>

            <!-- 4. Status-kolonner (4 bokse: 2x2 grid) -->
            <Grid Grid.Row="3" Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- 4.1 Requests (pending) -->
                <Border Grid.Row="0" Grid.Column="0"
                        Style="{StaticResource StatusColumnCardBorderStyle}"
                        Margin="0,0,16,16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <!-- Header -->
                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0"
                                    Style="{StaticResource StatusIconCircleStyle}"
                                    Background="{StaticResource PrimaryActionBrush}">
                                <TextBlock Text="🖥"
                                           FontSize="18"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center" />
                            </Border>

                            <TextBlock Grid.Column="1"
                                       Margin="12,0,0,0"
                                       VerticalAlignment="Center"
                                       FontFamily="{StaticResource PrimaryFont}"
                                       FontSize="16"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource PrimaryTextBrush}"
                                       Text="Pending requests" />

                            <Button Grid.Column="2"
                                    Margin="0,0,8,0"
                                    Content="{Binding PendingRequestsCount, StringFormat={}{0} items}"
                                    Style="{StaticResource PrimaryButtonStyle}"
                                    Background="{StaticResource PanelBackgroundBrush}"
                                    Foreground="{StaticResource PrimaryTextBrush}" />

                            
                        </Grid>

                        <ScrollViewer Grid.Row="1"
                                      Margin="0,12,0,0"
                                      VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding PendingRequests}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource StatusItemCardBorderStyle}">
                                            <Button Command="{Binding DataContext.OpenProcessRequestCommand, 
                                                                      RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Padding="0">
                                                <StackPanel>
                                                    <!-- Første linje: fx "REQ-501 ..." -->
                                                    <TextBlock Text="{Binding HeaderText}"
                                                               FontFamily="{StaticResource PrimaryFont}"
                                                               FontSize="13"
                                                               FontWeight="SemiBold"
                                                               Foreground="{StaticResource PrimaryTextBrush}" />
                                                    <!-- Anden linje: fx dato / ekstra info -->
                                                    <TextBlock Text="{Binding SubText}"
                                                               FontFamily="{StaticResource PrimaryFont}"
                                                               FontSize="12"
                                                               Foreground="{StaticResource SecondaryTextBrush}" />
                                                </StackPanel>
                                            </Button>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>

                <!-- 4.2 Planned -->
                <Border Grid.Row="0" Grid.Column="1"
                        Style="{StaticResource StatusColumnCardBorderStyle}"
                        Margin="16,0,0,16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0"
                                    Style="{StaticResource StatusIconCircleStyle}"
                                    Background="{StaticResource PrimaryActionBrush}">
                                <TextBlock Text="📱"
                                           FontSize="18"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center" />
                            </Border>

                            <TextBlock Grid.Column="1"
                                       Margin="12,0,0,0"
                                       VerticalAlignment="Center"
                                       FontFamily="{StaticResource PrimaryFont}"
                                       FontSize="16"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource PrimaryTextBrush}"
                                       Text="Planned" />

                            <Button Grid.Column="2"
                                    Margin="0,0,8,0"
                                    Content="{Binding PlannedDevicesCount, StringFormat={}{0} items}"
                                    Style="{StaticResource PrimaryButtonStyle}"
                                    Background="{StaticResource PanelBackgroundBrush}"
                                    Foreground="{StaticResource PrimaryTextBrush}" />

                            
                        </Grid>

                        <ScrollViewer Grid.Row="1"
                                      Margin="0,12,0,0"
                                      VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding PlannedDevices, Mode=OneWay}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource StatusItemCardBorderStyle}">
                                            <Button Command="{Binding DataContext.OpenUpdateDeviceCommand,
                                                                      RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Padding="0">
                                                <StackPanel>
                                                    <TextBlock Text="{Binding HeaderText}"
                                                               FontFamily="{StaticResource PrimaryFont}"
                                                               FontSize="13"
                                                               FontWeight="SemiBold"
                                                               Foreground="{StaticResource PrimaryTextBrush}" />
                                                    <TextBlock Text="{Binding SubText}"
                                                               FontFamily="{StaticResource PrimaryFont}"
                                                               FontSize="12"
                                                               Foreground="{StaticResource SecondaryTextBrush}" />
                                                </StackPanel>
                                            </Button>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>

                    </Grid>
                </Border>

                <!-- 4.3 Ordered -->
                <Border Grid.Row="1" Grid.Column="0"
                        Style="{StaticResource StatusColumnCardBorderStyle}"
                        Margin="0,16,16,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0"
                                    Style="{StaticResource StatusIconCircleStyle}"
                                    Background="{StaticResource PrimaryActionBrush}">
                                <TextBlock Text="📦"
                                           FontSize="18"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center" />
                            </Border>

                            <TextBlock Grid.Column="1"
                                       Margin="12,0,0,0"
                                       VerticalAlignment="Center"
                                       FontFamily="{StaticResource PrimaryFont}"
                                       FontSize="16"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource PrimaryTextBrush}"
                                       Text="Ordered" />

                            <Button Grid.Column="2"
                                    Margin="0,0,8,0"
                                    Content="{Binding OrderedDevicesCount, StringFormat={}{0} items}"
                                    Style="{StaticResource PrimaryButtonStyle}"
                                    Background="{StaticResource PanelBackgroundBrush}"
                                    Foreground="{StaticResource PrimaryTextBrush}" />


                       
                        </Grid>

                        <ScrollViewer Grid.Row="1"
                                      Margin="0,12,0,0"
                                      VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding OrderedDevices}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource StatusItemCardBorderStyle}">
                                            <Button Command="{Binding DataContext.OpenUpdateDeviceCommand,
                                                                      RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Padding="0">
                                                <StackPanel>
                                                    <TextBlock Text="{Binding HeaderText}"
                                                               FontFamily="{StaticResource PrimaryFont}"
                                                               FontSize="13"
                                                               FontWeight="SemiBold"
                                                               Foreground="{StaticResource PrimaryTextBrush}" />
                                                    <TextBlock Text="{Binding SubText}"
                                                               FontFamily="{StaticResource PrimaryFont}"
                                                               FontSize="12"
                                                               Foreground="{StaticResource SecondaryTextBrush}" />
                                                </StackPanel>
                                            </Button>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>

                <!-- 4.4 Ready to approve / assign -->
                <Border Grid.Row="1" Grid.Column="1"
                        Style="{StaticResource StatusColumnCardBorderStyle}"
                        Margin="16,16,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0"
                                    Style="{StaticResource StatusIconCircleStyle}"
                                    Background="{StaticResource PrimaryActionBrush}">
                                <TextBlock Text="✔"
                                           FontSize="18"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center" />
                            </Border>

                            <TextBlock Grid.Column="1"
                                       Margin="12,0,0,0"
                                       VerticalAlignment="Center"
                                       FontFamily="{StaticResource PrimaryFont}"
                                       FontSize="16"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource PrimaryTextBrush}"
                                       Text="Ready to assign" />

                            <Button Grid.Column="2"
                                    Margin="0,0,8,0"
                                    Content="{Binding ReceivedDevicesCount, StringFormat={}{0} items}"
                                    Style="{StaticResource PrimaryButtonStyle}"
                                    Background="{StaticResource PanelBackgroundBrush}"
                                    Foreground="{StaticResource PrimaryTextBrush}" />


                            
                        </Grid>

                        <ScrollViewer Grid.Row="1"
                                      Margin="0,12,0,0"
                                      VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding ReceivedDevices}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource StatusItemCardBorderStyle}">
                                            <Button Command="{Binding DataContext.OpenUpdateDeviceCommand,
                                                                      RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Padding="0">
                                                <StackPanel>
                                                    <TextBlock Text="{Binding HeaderText}"
                                                               FontFamily="{StaticResource PrimaryFont}"
                                                               FontSize="13"
                                                               FontWeight="SemiBold"
                                                               Foreground="{StaticResource PrimaryTextBrush}" />
                                                    <TextBlock Text="{Binding SubText}"
                                                               FontFamily="{StaticResource PrimaryFont}"
                                                               FontSize="12"
                                                               Foreground="{StaticResource SecondaryTextBrush}" />
                                                </StackPanel>
                                            </Button>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>

                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Viewbox>

       <!-- <local:OverlayHost DataContext="{Binding}" /> -->
    </Grid>

</UserControl>
