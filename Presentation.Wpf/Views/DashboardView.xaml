<UserControl x:Class="Presentation.Wpf.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Presentation.Wpf.Views"
             xmlns:dash="clr-namespace:Presentation.Wpf.Views.DashboardUserControls"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="1000"
             Background="{StaticResource PageBackgroundBrush}">

    <!-- Root grid  -->
    <Grid x:Name="Root">

        <!-- Inner grid  -->
        <Grid Margin="24,0,24,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <!-- Quick actions sektion -->
                <RowDefinition Height="*"   />
                <!-- Tasks sektion -->
                <RowDefinition Height="Auto"/>
                <!-- EmployeeTerminationList sektion -->
            </Grid.RowDefinitions>

            <!-- 1. Quick actions user control -->
            <dash:QuickActionsUserControl Grid.Row="0" Margin="0,0,0,16"/>

            <!-- 2. Tasks user control -->
            <dash:TasksUserControl Grid.Row="1"/>

            <!--  Terminate employee user control -->
            <!--
            <dash:HandleTerminationUserControl Grid.Row="2" Margin="0,24,0,0"/>
            -->
        </Grid>

        <local:OverlayHost DataContext="{Binding}" />

    </Grid>
</UserControl>
