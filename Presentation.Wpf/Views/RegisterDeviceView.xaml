<UserControl x:Class="Presentation.Wpf.Views.RegisterDeviceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.Wpf.Views"
             mc:Ignorable="d" 
             Background="{StaticResource PageBackgroundBrush}"
             d:DesignHeight="800" d:DesignWidth="450">
    
    <Grid Margin="20" Background="White">
        <Grid.RowDefinitions>
            <!-- Header-->
            <RowDefinition Height="auto"/>
            <!-- Form-->
            <RowDefinition Height="auto"/>
            <!-- Footer-->
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header-->
        <TextBlock Text="Register device"
                   Style="{StaticResource HeaderTextStyle}"
                   Margin="0,0,0,20"
                   Grid.Row="0"/>

        <!-- Form-->
        <Grid Grid.Row="1" Margin="0,0,0,0">
            <Grid.RowDefinitions>
                <!-- Type-->
                <RowDefinition Height="auto"/>
                <!-- OS-->
                <RowDefinition Height="auto"/>
                <!-- Location-->
                <RowDefinition Height="auto"/>
                <!-- Dates-->
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <!-- Type-->
            <StackPanel Grid.Row="0" HorizontalAlignment="Stretch">
                <TextBlock Text="Device Type" 
                           Style="{StaticResource BaseTextStyle}"
                           FontWeight="SemiBold"
                           Margin="0,0,0,4"/>
                <ComboBox ItemsSource="{Binding DeviceTypeOptions}"
                          SelectedItem="{Binding SelectedDeviceType, Mode=TwoWay}"
                          Style="{StaticResource InputFieldComboStyle}"
                          Margin="0,0,0,12"/>
            </StackPanel>
            
            <!-- OS-->
            <StackPanel Grid.Row="1" HorizontalAlignment="Stretch">
                <TextBlock Text="Operative System" 
                           Style="{StaticResource BaseTextStyle}"
                           FontWeight="SemiBold"
                           Margin="0,0,0,4"/>
                <ComboBox ItemsSource="{Binding OSOptions}"
                          SelectedItem="{Binding SelectedOS, Mode=TwoWay}"
                          Style="{StaticResource InputFieldComboStyle}"
                          Margin="0,0,0,12"/>

            </StackPanel>

            <!-- Location-->
            <StackPanel Grid.Row="2" HorizontalAlignment="Stretch">
                <TextBlock Text="Location (country)" 
                           Style="{StaticResource BaseTextStyle}"
                           FontWeight="SemiBold"
                           Margin="0,0,0,4"/>
                <ComboBox ItemsSource="{Binding CountryOptions}"
                          SelectedItem="{Binding SelectedCountry, Mode=TwoWay}"
                          Style="{StaticResource InputFieldComboStyle}"
                          Margin="0,0,0,12"/>

            </StackPanel>

            <!-- Dates-->
            <Grid Grid.Row="3" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Registration date-->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Registration date"
                               Style="{StaticResource BaseTextStyle}"
                               FontWeight="SemiBold"
                               Margin="0,0,0,4"/>
                    <DatePicker SelectedDate="{Binding RegistrationDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                Height="33"
                                Background="{StaticResource InputFieldBackgroundBrush}"
                                BorderBrush="{StaticResource InputFieldBorderBrush}"/>
                </StackPanel>

                <!-- Expiry date-->
                <StackPanel Grid.Column="1" >
                    <TextBlock Text="Expiry date"
                               Style="{StaticResource BaseTextStyle}"
                               FontWeight="SemiBold"
                               Margin="0,0,0,4"/>
                    <DatePicker SelectedDate="{Binding ExpiryDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                Height="33"
                                Background="{StaticResource InputFieldBackgroundBrush}"
                                BorderBrush="{StaticResource InputFieldBorderBrush}"/>
                </StackPanel>                
            </Grid>
            
            


        </Grid>

        <!-- Footer -->
        <Grid Grid.Row ="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="1" 
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">

                <Button Content="Cancel"
                        Style="{StaticResource RejectButtonStyle}"
                        Margin="0,0,8,0"
                        Command="{Binding CancelCommand}"/>

                <Button Content="Register"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding RegisterCommand}"/>
            </StackPanel>
        </Grid>



    </Grid>
</UserControl>
