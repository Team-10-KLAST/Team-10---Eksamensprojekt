<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Input field styles -->
    <Style TargetType="TextBox" x:Key="InputFieldStyle">
        <Setter Property="Background" Value="{DynamicResource InputFieldBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource InputFieldBorderBrush}" />
        <Setter Property="Padding" Value="4" />
        <!-- Mindre padding -->
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFont}" />
        <Setter Property="FontSize" Value="14" />
        <!-- Matcher tekststørrelse -->
        <Setter Property="Height" Value="28" />
        <!-- Mindre højde -->
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <!-- Readonly Input Field Style -->
    <Style x:Key="ReadonlyInputFieldStyle" TargetType="TextBox" BasedOn="{StaticResource InputFieldStyle}">
        <Setter Property="Background" Value="{DynamicResource ReadonlyInputBackgroundBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
        <Setter Property="IsReadOnly" Value="True" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="BorderBrush" Value="{DynamicResource InputFieldBorderBrush}" />
    </Style>

    <!-- ComboBox style -->
    <Style TargetType="ComboBox" x:Key="InputFieldComboStyle">
        <Setter Property="Background" Value="{DynamicResource InputFieldBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource InputFieldBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFont}" />
        <Setter Property="Height" Value="30" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource PrimaryActionHoverBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ToggleButton style (styrer pilen i ComboBox) -->
    <Style TargetType="ToggleButton">
        <Setter Property="Background" Value="{DynamicResource InputFieldBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource InputFieldBorderBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Height" Value="30" />
        <Setter Property="Padding" Value="5" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource PrimaryActionHoverBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Search input base style -->
    <Style x:Key="SearchInputBaseStyle" TargetType="Control">
        <Setter Property="Height" Value="30" />
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFont}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
        <Setter Property="Background" Value="{DynamicResource InputFieldBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource InputFieldBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="4,0" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <!-- Search input TextBox style -->
    <Style x:Key="SearchInputTextBoxStyle"
   TargetType="TextBox"
   BasedOn="{StaticResource SearchInputBaseStyle}">
    </Style>

    <!-- Search input ComboBox style -->
    <Style x:Key="SearchInputComboBoxStyle"
   TargetType="ComboBox"
   BasedOn="{StaticResource SearchInputBaseStyle}">
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="IsEditable" Value="False" />
    </Style>
</ResourceDictionary>